
<div class="rich-text__mini-product flex justify-inherit media-banner__pointer mini-product__wrapper mini-product__wrapper--{{ block.settings.style }}" {{  block.shopify_attributes }}>
  {%- case block.settings.style -%}
    {%- when 'card' -%}
      {% render 'mini-product-card', scoped_settings: block.settings %}
    {%- when 'list' -%}
      {% render 'mini-product-list', scoped_settings: block.settings %}
  {%- endcase -%}
</div>

{% style %}
.rich-text__mini-product .price {
  font-family: var(--font-body-family);
  font-style: var(--font-body-style);
  font-weight: var(--font-body-weight);
  font-size: var(--font-body-size);
  text-transform: none;
  letter-spacing: var(--font-body-letter-spacing);
}
{% endstyle %}

{% schema %}
{
  "name": "t:mini-product",
  "tag": null,
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "t:product"
    },
    {
      "type": "select",
      "id": "style",
      "default": "card",
      "options": [
        {
          "label": "t:card",
          "value": "card"
        },
        {
          "label": "t:list",
          "value": "list"
        }
      ],
      "label": "t:style"
    },
    {
      "type": "header",
      "content": "t:card-style"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:color-scheme",
      "default": "scheme-1"
    }
  ],
  "presets": [
    {
      "name": "t:mini-product"
    }
  ]
}
{% endschema %}